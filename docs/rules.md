- Wrap all element withing in root element.(render function only returns root element.
```
render() {
    return (
      <div className="App">
        <h1>Hello React.</h1>
      </div>
    );
}
```
- src folder create your componet folder with capital name
- Component file with capital 
- function with lower case